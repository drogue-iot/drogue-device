[build]

target = "thumbv6m-none-eabi"
rustflags = [
  "-C", "link-arg=-Tlink.x",
  #"-C", "inline-threshold=275",
  # "-C", "link-arg=-Tlink.x",
  # "-C", "linker=cortex-m-rt-ld", # <- CHANGED!
  # "-Z", "linker-flavor=ld",
  # "-Z", "thinlto=no",

]
